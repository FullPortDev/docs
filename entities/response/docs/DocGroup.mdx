---
title: Doc Group
description: EzLLM Doc Group
---

A single document grouping

# Properties
<ParamField path="id" type="str">
    id of the grouping
    
    `document_id` if a document grouping
    
    `collection_id` if a collection grouping

    `None` if an all grouping
</ParamField>
<ParamField path="type" type="str">
    grouping type, `all`, `collection`, `document`
</ParamField>
<ParamField path="docs" type="ResponseDocs">
    [ResponseDocs](<./ResponseDocs>) object
</ParamField>